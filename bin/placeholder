#!/usr/bin/env ruby

require 'rubygems'

$: << File.join(File.dirname(__FILE__), "..", "lib")
require 'placeholder/cli/command'

# Setup the app to work from a base Runner/Command etc.
Placeholder::MainCommand.run